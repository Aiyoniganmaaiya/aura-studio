# Verified against the reference dev environment (Windows 11, Python 3.13,
# RTX 4060 8 GB) on 2026-08-22. Z-Image GGUF support needs diffusers >= 0.36
# and the `gguf` package; FLUX/SD paths are regression-checked on these pins.
fastapi>=0.115.0
uvicorn>=0.30.1
# WebSocket protocol implementation for uvicorn (plain `uvicorn` does NOT
# include one — without it /ws/generate answers "Unsupported upgrade request")
websockets>=12.0
diffusers>=0.40.0
torch>=2.6.0
transformers>=5.14.0
accelerate>=1.14.0
huggingface-hub>=1.23.0
gguf>=0.10.0
pillow>=10.4.0
safetensors>=0.8.0
pydantic>=2.8.0
# Canny preprocessing for ControlNet (headless: no GUI deps needed server-side)
opencv-python-headless>=4.9.0
